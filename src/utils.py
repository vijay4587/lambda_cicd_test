def process_data(name: str) -> str:
    """
    Sample utility function that processes data.
    For now, it just converts the name to uppercase and appends text.
    """
    return f"{name.upper()}-PROCESSED"
